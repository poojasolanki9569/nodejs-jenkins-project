pipeline {
  agent any
  stages {
    stage('Pre-build') {
      steps {
        echo 'Pre-build, checkout'
      }
    }

    stage('build') {
      steps {
        echo 'build'
      }
    }

    stage('deploy') {
      steps {
        echo 'deploy'
      }
    }

  }
}